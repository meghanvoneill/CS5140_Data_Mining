

def main():

    domain_size_n = 300

    ##############################################################################
    # Generate random numbers in the domain [n] until every value i in [n] has had
    # one random number equal to i. How many random trials did this take? We will
    # use k to represent this value.

    ##############################################################################
    # Repeat the previous step for m = 400 times, and for each repetition, record
    # the value k of how many random trials were required to collect all values i
    # in [n]. Make a cumulative density plot.

    ##############################################################################
    # Use the above results to calculate the empirical expected value of k.

    ##############################################################################
    # Describe how you implemented this experiment and how long it took for
    # n = 300 and m = 400 trials.
    # Show a plot of the run time as you gradually increase the parameters n and m.
    # (For at least 3 fixed values of m between 400 and 5,000, plot the time as a
    # function of n.) You should be able to reach n = 20,000 and m = 5,000.

    return


if __name__ == '__main__':
    main()
